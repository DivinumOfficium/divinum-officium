apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

commonLabels:
  app: divinum-officium
  managed-by: argocd

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# You can add configuration overrides here
# For example:
# images:
#   - name: ghcr.io/divinumofficium/divinum-officium
#     newTag: v1.2.3  # Override the image tag

# configMapGenerator:
#   - name: app-config
#     literals:
#       - KEY=VALUE
